---
import I18n from "i18next";
interface Props {
  page: number;
}
const { page } = Astro.props;
---

<span class="prev">
  <a
    rel="prev"
    href={`?p=${page - 1}`}
    set:html={I18n.t("views.pagination.previous", "&lsaquo; Prev")}
  />
</span>
