---
import { buildPageLink } from "./common";
interface Props {
  i: number;
  page: number;
}
const { i, page } = Astro.props;
---

<span class={`page ${i === page ? "current" : ""}`}>
  {i === page ? i : <a href={buildPageLink(Astro, i)}>{i}</a>}
</span>
