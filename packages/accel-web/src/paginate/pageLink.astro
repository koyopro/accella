---
interface Props {
  i: number;
  page: number;
}
const { i, page } = Astro.props;
---

<span class={`page ${i === page ? "current" : ""}`}>
  {i === page ? i : <a href={`?p=${i}`}>{i}</a>}
</span>
